# CI is temporarily disabled while the initial implementation is being stabilized.
# Restore the workflow trigger when local verification and integration tests are ready.

name: CI (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI is temporarily disabled; run local tests on feature/develop."
