# Sales Data Analysis

Use the Database MCP Server for schema discovery and read-only analytical SQL.

## Tool policy

- Discover schema before constructing unfamiliar queries.
- Default to read-only SQL.
- Bound result sizes and execution time.
- Preserve database source metadata.
